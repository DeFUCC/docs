{% extends 'partials/base.html.twig' %}

{% block content %}

    <div id="body-inner" class="columns is-multiline">
      <div class="column is-half">
        {{ page.media.images|first|html }}
      </div>
      <div class="column content is-half">
        <h1>{{ page.title }}</h1>
        <p>
          {{ page.content }}
        </p>
      </div>

    </div>

{% endblock %}
