{% if item.visible %}

            <a href="{{item.url}}" class="card item-card">


            	   {% include 'partials/card-image.html.twig'%}

            	    <div class="card-content">


                        <div class="content">
                            <h4 class="title is-size-4">{{item.title}}
                            </h4>

                            <p class="subtitle is-size-7">

                                    <p>{{item.summary()|striptags}}
                                    </p>

                        </div>
            	    </div>
              </a>



{% endif %}
