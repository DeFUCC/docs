
{% set tags = page.taxonomy.tag %}
{% set cats = page.taxonomy.category %}

{% if tags %}
    {% set progress = page.collection({'items':{'@taxonomy':{'category': cats, 'tag': tags}},'order': {'by': 'default', 'dir': 'asc'}}) %}
{% else %}
    {% set progress = page.collection({'items':{'@taxonomy':{'category': cats}},'order': {'by': 'default', 'dir': 'asc'}}) %}
{% endif %}


	<div id="navigation">
	{% if not progress.isFirst(page.path) %}
	    <a class="nav nav-prev" href="{{ progress.nextSibling(page.path).url }}"> <i class="fa fa-2x fa-chevron-left"></i></a>
	{% endif %}

	{% if not progress.isLast(page.path) %}
	    <a class="nav nav-next" href="{{ progress.prevSibling(page.path).url }}"><i class="fa fa-2x fa-chevron-right"></i></a>
	{% endif %}
	</div>

