title: Article
'@extends':
    type: page
    context: theme://blueprints

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        content:
          fields:

            columns:
              type: columns
              fields:

                column2:
                  type: column
                  fields:

                    header.subtitle:
                      ordering@: -1
                      style: vertical
                      type: textarea
                      rows: 4
                      label: Subtitle
