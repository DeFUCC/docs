

  <a class="button {{ item.disabled ? 'disabled' : '' }}" href="{{item.link}}" target="_blank">
    {% if item.icon %}
      <i class="{{item.icon}}"></i>
    {% endif %}
    <strong>{{item.verb}}</strong> {{item.text}}
   </a>
