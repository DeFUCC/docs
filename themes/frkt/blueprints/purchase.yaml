title: Purchase
'@extends':
    type: mainpro
    context: theme://blueprints

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        content:
          fields:

            columns:
              type: columns
              fields:

                column1:
                  type: column
                  fields:

                    fincols:
                      type: columns
                      ordering@: 0
                      label: Сбор средств
                      fields:

                        fincol1:
                          type: column
                          fields:

                            header.finance.total:
                              style: vertical
                              type: number
                              label: THEME_FRUKT.TOTAL

                        fincol2:
                          type: column
                          fields:

                            header.finance.available:
                              style: vertical
                              type: number
                              label: THEME_FRUKT.AVAILABLE
